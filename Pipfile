[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
ipython = ">=7.7.0"
ipdb = ">=0.12"
pylint = ">=2.3.1"
coverage = ">=4.5.4"
pytest = ">=6.0.0"
pytest-cov = ">=2.7.1"
pytest-random-order = ">=1.0.4"
hypothesis = ">=4.32.3"
pytest-localserver = ">=0.5.0"
ordered-set = ">=4.0.2"
pre-commit = ">=1.18.1"
twine = ">=3.1.0"
cloudformation-cli = {editable = true,path = "."}

[requires]
python_version = "3.7"
